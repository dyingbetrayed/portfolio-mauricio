{
  "name": "portfolio-mauro",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "sync-media": "node scripts/sync-media.js",
    "prebuild": "npm run sync-media && node scripts/optimize-images.js && node scripts/generate-og.js",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^7.1.3"
  },
  "devDependencies": {
    "sharp": "^0.35.4"
  }
}
